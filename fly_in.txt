colocar no def_run, logo abaixo do else


if capacity_info:
    print(f"Turn: {self._current_turn}")
    for zone_name, zone in self.zones.items():
        current_drones = sum(1 for d in self.drones if d.current_zone.name == zone_name)
        max_drones = getattr(zone, 'max_drones', 'inf')
        if max_drones != 'inf' or current_drones > 0:
            status = "Full" if max_drones != 'inf' and current_drones >= max_drones else ""
            print(f"Zone [{zone_name}]: {current_drones}/{max_drones} drones {status}")
    print("\n" + "-" * 40 + "\n")



colocar no topo da funcao:
	capacity_info: bool = False
